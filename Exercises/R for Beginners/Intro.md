Creating, listing and deleting the objects in memeory

n <- 
  -> n

rnorm(1) generates a normal random variate with mean zero and variance unity.

ls() lists simply the objects in memory

ls(pat = "") option pattern lists only the objects which  contain a given character in their name

ls.str() displays some details on the objects in memory

rm() deletes the object

